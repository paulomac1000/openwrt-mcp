services:
  openwrt-mcp:
    build: .
    env_file: .env
    ports:
      - "9094:9094"   # health
      - "9095:9095"   # MCP SSE
      - "9096:9096"   # REST API
    volumes:
      - /path/to/ssh/keys:/app/keys:ro
    restart: unless-stopped