{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Write Command",
  "description": "Payload schema for write tools (restart_interface, reload_network). Requires ENABLE_WRITE_OPERATIONS=1.",
  "type": "object",
  "properties": {
    "interface_name": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9._-]{0,14}$",
      "description": "Network interface name (e.g., 'wan', 'lan', 'wwan0'). Only for restart_interface."
    }
  }
}
